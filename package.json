{
  "name": "npm-for-all",
  "version": "0.0.1",
  "description": "A journey through npm development capabilities",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm i && browserify -r ./widget.js:widget -o dist.js",
    "start": "npm run build && browser-sync start --server"
  },
  "keywords": [
    "npm",
    "tutorial"
  ],
  "author": "Santiago Bernabé",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.4.0",
    "mocha": "^2.2.1",
    "tape": "^3.5.0"
  },
  "dependencies": {
    "browserify": "^9.0.3",
    "domify": "^1.3.2",
    "english-time": "0.0.9"
  }
}
